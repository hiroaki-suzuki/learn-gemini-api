# GEMINI.md

## Project Overview

This is a Python project named "learn-gemini-api". Based on the file structure and the name, it is likely a project for learning and experimenting with the Gemini API. The project is in its early stages, with a simple "Hello from learn-gemini-api!" entry point in `main.py`.

The project uses a `pyproject.toml` file for managing dependencies and project metadata, which is standard for modern Python projects.

The presence of `AGENTS.md` and `CLAUDE.md` suggests that this project may involve working with AI agents, possibly from different providers.

## Building and Running

### Prerequisites

*   Python 3.14 or greater

### Installation

To install the dependencies, you can use a package manager that supports `pyproject.toml`, such as `pip`:

```bash
pip install .
```

### Running the application

To run the main application, execute the `main.py` file:

```bash
python main.py
```

### Testing

TODO: Add instructions on how to run tests once a testing framework is in place.

## Development Conventions

### Code Style

TODO: Add information about code style guidelines once they are established. For example, using a linter like `ruff` or a formatter like `black`.

### Contribution Guidelines

TODO: Add contribution guidelines if this project will be worked on by multiple people.
